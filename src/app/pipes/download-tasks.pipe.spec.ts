import { DownloadTasksPipe } from './download-tasks.pipe';

describe('DownloadTasksPipe', () => {
  it('create an instance', () => {
    const pipe = new DownloadTasksPipe();
    expect(pipe).toBeTruthy();
  });
});

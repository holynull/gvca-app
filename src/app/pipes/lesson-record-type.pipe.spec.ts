import { LessonRecordTypePipe } from './lesson-record-type.pipe';

describe('LessonRecordTypePipe', () => {
  it('create an instance', () => {
    const pipe = new LessonRecordTypePipe();
    expect(pipe).toBeTruthy();
  });
});

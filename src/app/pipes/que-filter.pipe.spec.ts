import { QueFilterPipe } from './que-filter.pipe';

describe('QueFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new QueFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
